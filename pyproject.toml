[project]
name = "dycomutils"
version = "0.3.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jsonlines>=4.0.0",
    "pynput>=1.8.1",
    "tqdm>=4.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
